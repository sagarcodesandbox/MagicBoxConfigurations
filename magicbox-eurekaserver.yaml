# In the Git repository, for eurekaserver's config
server:
  port: 8070

eureka:
  instance:
    hostname: localhost
  client:
    fetchRegistry: true       # Change this
    registerWithEureka: true  # Change this
    serviceUrl:
      defaultZone: http://localhost:8070/eureka/ # <-- HARDCODE THIS FOR TESTING